[project]
name = "reforma-api-ml"
version = "0.1.0"
description = "Microservicio ML REFORMA"
requires-python = ">=3.12"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.pytest.ini_options]
testpaths = ["tests"]
