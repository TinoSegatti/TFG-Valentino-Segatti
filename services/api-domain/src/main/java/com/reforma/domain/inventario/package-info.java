/**
 * Modulo de inventario (RF-INV).
 * Calcula cantidad acumulada, cantidad sistema, merma, valor de stock y precio almacen
 * (promedio ponderado SUM(cantidad x precio) / SUM(cantidad)) a partir de compras,
 * fabricaciones (futuro) e inicializacion manual.
 */
package com.reforma.domain.inventario;
