spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration

reforma:
  jwt:
    secret: test_jwt_secret_minimo_64_caracteres_para_integracion_reforma_catalogos_it
    expiration-hours: 1
  ml:
    base-url: http://localhost:8081
    secret: test_ml_jwt_secret_minimo_64_caracteres_para_integracion_reforma_catalogos
  frontend-url: http://localhost:4200
